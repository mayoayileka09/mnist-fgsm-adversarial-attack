[project]
name = "mnist-fgsm-adversarial-attack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    
    "numpy<2",
    "torch==2.2.2",
    "torchvision==0.17.2",
]
